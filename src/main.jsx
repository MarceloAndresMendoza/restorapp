import React from 'react'
import ReactDOM from 'react-dom/client'
import Restorapp from './Restorapp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Restorapp />
  </React.StrictMode>,
)
